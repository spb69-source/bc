# MongoDB Configuration
# For local MongoDB (default)
MONGODB_URI=mongodb://localhost:27017/bank-sync-app

# For MongoDB Atlas (replace with your actual connection string)
# MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/bank-sync-app?retryWrites=true&w=majority

# Other environment variables
NODE_ENV=development